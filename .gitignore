# Secrets — WiFi + Firebase credentials (see second_esp32/Secrets.h.example)
second_esp32/Secrets.h

# Local Claude Code tool config (machine-specific, not project source)
.claude/

# Old test sketches / legacy prototype — not part of the repo
/tests/

# Local-only docs — kept on disk, not published to GitHub
/הסבר_מקיף_לפרויקט.md
/סיכום_חיישנים_ומנועים.docx

# Node
node_modules/
dist/
dist-ssr/

# OS / editor
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea
