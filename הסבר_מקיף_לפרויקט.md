# AutoBraid — הסבר מקיף להגשת הפרויקט

> קובץ זה נועד לתת לך שליטה מלאה בכל חלקי הפרויקט: איך כל חיישן עובד, מה כל שורת קוד עושה, איך שני הלוחות מדברים ביניהם (UART), ואיך הכל מתחבר לענן (Firebase) ולאפליקציית React.

---

## 1. תמונת מצב כללית — מבנה הפרויקט

הפרויקט מורכב **משני לוחות ESP32 נפרדים** שמתקשרים ביניהם בכבל (UART), ולוח אחד מדבר גם עם האינטרנט (WiFi + Firebase):

```
┌─────────────────────┐        UART (כבל, 2 חוטים + GND)        ┌─────────────────────┐
│   ESP32 "ראשון"      │ <──────────────────────────────────────> │   ESP32 "שני" (המוח)  │
│   לוח החיישנים        │        Serial2, 115200 baud              │   מסך + מנועים        │
│                      │                                          │                      │
│  • כפתור חירום        │                                          │  • מסך מגע TFT9341    │
│  • חיישן אולטרסוני    │                                          │  • מנוע מסילה         │
│    HC-SR04           │                                          │  • מנוע קליעה         │
│  • חיישן צבע TCS3200 │                                          │  • WiFi + Firebase    │
│  • מנוע קרוסלת        │                                          │    (Firestore)        │
│    תוספות (סטפר)      │                                          │                      │
└─────────────────────┘                                          └──────────┬───────────┘
                                                                              │ WiFi
                                                                              ▼
                                                                    ┌──────────────────┐
                                                                    │  Firebase Cloud   │
                                                                    │  Firestore        │
                                                                    │  (codes/orders)   │
                                                                    └──────────────────┘
```

**תיקיות הפרויקט (בשורש):**

| תיקייה | תפקיד |
|---|---|
| `first_esp32/` | **הלוח הראשון (Slave)** — כל החיישנים + מנוע התוספות. הקוד הפעיל בפרויקט הנוכחי. |
| `second_esp32/` | **הלוח השני (Master)** — המסך, שני מנועי הצעד הנוספים, החיבור ל-WiFi/Firebase, וכל הלוגיקה הראשית. הקוד הפעיל בפרויקט הנוכחי. |
| `esp32_firebase/` | גרסה **ישנה/חלופית** של לוח אחד עם מסך + שרת HTTP לתקשורת עם אפליקציית React (לא בשימוש בגרסה הנוכחית עם שני הלוחות). |
| `pin_test/`, `esp32_board_test/`, `uart_hello_test/` | סקיצות בדיקה קטנות (בדיקת פינים, בדיקת לוח, בדיקת UART) — לצורך פיתוח בלבד. |
| `react-app/` | אפליקציית ניהול/הזמנות מבוססת React + Firebase (ממשק משתמש נפרד מה-ESP32). |

מכיוון שהשאלה שלך היא על **החיישנים**, המיקוד העיקרי כאן הוא `first_esp32/` (שם כל החיישנים חיים), אבל אני מסביר גם איך `second_esp32/` "מזמין" מהם מידע, כדי שתביני את כל שרשרת התקשורת מקצה לקצה.

---

## 2. כל החיישנים — מה הם ואיך הם עובדים

כל החיישנים מחוברים ל**לוח הראשון** בלבד (`first_esp32`). הם קבועים בקובץ `first_esp32/Config.h`:

```cpp
#define BUTTON_PIN   27   // כפתור התחלה/חירום
#define TRIG_PIN     21   // אולטרסוני - שולח פולס
#define ECHO_PIN     19   // אולטרסוני - מקבל הד
#define S0 23  #define S1 22   // חיישן צבע - בחירת סקאלת תדר
#define S2 32  #define S3 33   // חיישן צבע - בחירת פילטר צבע
#define COLOR_OUT 35            // חיישן צבע - יציאת התדר הנמדד
```

### 2.1 כפתור התחלה/חירום (`buttom_switch.ino`)

**מה זה עושה בפועל:** כפתור לחיצה פיזי יחיד שממלא **שני תפקידים** לפי ההקשר — לפני קליעה הוא כפתור "אישור/OK", ובזמן קליעה פעילה הוא הופך לכפתור "עצירת חירום".

```cpp
void buttonSetup() {
  pinMode(BUTTON_PIN, INPUT_PULLUP);// נגד משיכה פנימי מעלה
}
bool buttonPressed() {
  return digitalRead(BUTTON_PIN) == LOW;  // לחוץ => LOW
}
```

- `INPUT_PULLUP` — מפעיל נגד פנימי בתוך השבב שמושך את הפין ל-HIGH (5/3.3V) כברירת מחדל. כשלוחצים על הכפתור, הוא **מחבר את הפין ל-GND**, ולכן הקריאה הופכת ל-`LOW`.
- למה pull-up ולא pull-down? כי כך אפשר לחבר את הכפתור בשני חוטים בלבד (פין + GND) בלי נגד חיצוני — הפין "יודע" לבד להיות HIGH במנוחה.
- `buttonPressed()` פשוט מחזיר `true` אם הפין קרוא כ-`LOW`, כלומר הכפתור לחוץ **ברגע הקריאה הזה בדיוק** (לא "נלחץ פעם", אלא "לחוץ עכשיו").

**איך הכפתור באמת מזוהה כ"לחיצה חדשה" (בדיבאונס)** — זה קורה לא בקובץ הזה אלא ב-`first_esp32.ino`, בפונקציה `handleButton()`:

```cpp
void handleButton() {
  bool pressed = buttonPressed();

  if (pressed && !lastBtnPressed && (millis() - lastBtnChange > BTN_DEBOUNCE_MS)) {
    lastBtnChange = millis();
    if (armed) {
      sendLine("EMG");     // באמצע קליעה => שולח חירום ללוח השני
    } else {
      sendLine("OK");      // לא באמצע קליעה => שולח "אישור"
    }
  }
  lastBtnPressed = pressed;
}
```

- `pressed && !lastBtnPressed` — זה התנאי החשוב: זה בודק שהכפתור **עבר** ממצב "לא לחוץ" למצב "לחוץ" בסבב הזה בדיוק (edge detection / קצה עולה של הלחיצה). כך לחיצה ארוכה לא נספרת פעמיים.
- `millis() - lastBtnChange > BTN_DEBOUNCE_MS` — דיבאונס (debounce): כפתורים מכניים "מרעידים" חשמלית כמה מילישניות בזמן הלחיצה/שחרור בגלל מגע פיזי לא מושלם, ולכן בלי הגנה זו אפשר לקבל כמה "לחיצות" מזויפות מלחיצה אחת. `BTN_DEBOUNCE_MS` מוגדר כ-50 מ"ש ב-`Config.h`.
- **חשוב:** `armed` הוא משתנה גלובלי שמשקף אם המערכת "במצב קליעה" כרגע. הוא נשלט מרחוק ע"י הלוח השני דרך פקודות `ARMED`/`DISARM` (ראי סעיף 3). כך אותו כפתור פיזי מתפקד כ"OK" לפני הקליעה ו"EMERGENCY" בזמנה — **בלי חיווט נוסף**, רק בזכות ההיגיון בתוכנה.

### 2.2 חיישן אולטרסוני HC-SR04 — מדידת מרחק (`Ultrasonic_esp32.ino`)

**תפקיד:** בודק שהראש/השיער של המשתמשת נמצאים במרחק תקין מהמכונה לפני שמתחילים לקלוע (בין 7 ל-16 ס"מ, לפי `DIST_MIN_CM`/`DIST_MAX_CM` ב-`Config.h`).

**עקרון הפעולה הפיזי:** החיישן שולח פולס קול אולטרה-סוני (לא נשמע לאוזן אנושית) מרגל TRIG, הקול מתנגש במכשול וחוזר כהד לרגל ECHO. מודדים כמה זמן עבר בין השליחה לקבלת ההד, וממירים לזמן-מרחק לפי מהירות הקול.

```cpp
void ultrasonicSetup() {
  pinMode(TRIG_PIN, OUTPUT);        // TRIG = יציאה (אנחנו שולחים ממנה)
  pinMode(ECHO_PIN, INPUT);         // ECHO = כניסה (אנחנו קוראים ממנה)
  digitalWrite(TRIG_PIN, LOW);      // מתחיל במצב נמוך
}

float readDistanceCm() {
  digitalWrite(TRIG_PIN, LOW);
  delayMicroseconds(2);             // ייצוב קצר לפני הפולס
  digitalWrite(TRIG_PIN, HIGH);     // מתחיל את פולס הטריגר
  delayMicroseconds(10);            // 10 מיקרו-שניות — כך דורש דף הנתונים (datasheet) של החיישן
  digitalWrite(TRIG_PIN, LOW);      // מסיים את הפולס

  long duration = pulseIn(ECHO_PIN, HIGH, 30000);  // מודד כמה זמן ECHO נשאר HIGH (עד 30 מ"ש timeout)
  if (duration == 0) {              // pulseIn מחזיר 0 אם לא נמדד דבר בזמן הזה
    return -1.0;                    // כלומר "אין הד" = שום דבר לא מספיק קרוב
  }

  float dist = duration * 0.0343 / 2.0;  // הנוסחה המרכזית
  bool ok = (dist >= DIST_MIN_CM && dist <= DIST_MAX_CM);
  return dist;
}
```

**הסבר שורה-שורה על הנוסחה `duration * 0.0343 / 2.0`:**
- מהירות הקול באוויר היא בערך 343 מטר לשנייה = **0.0343 ס"מ למיקרו-שנייה**.
- `duration` (במיקרו-שניות) הוא הזמן שלקח לקול **ללכת ולחזור** (הלוך ושוב).
- `duration * 0.0343` נותן את **המרחק הכולל** שהקול עבר (הלוך + חזור).
- מחלקים ב-2 כדי לקבל רק את המרחק **בכיוון אחד** — כלומר המרחק בין החיישן למכשול.
- `pulseIn(ECHO_PIN, HIGH, 30000)` — פונקציית ליבה של Arduino שמחכה שהפין יעלה ל-HIGH, מודדת כמה זמן הוא נשאר HIGH, ומחזירה את הזמן במיקרו-שניות. הפרמטר השלישי (30000) הוא timeout — אם תוך 30 מ"ש לא קורה כלום, מפסיקים לחכות ומחזירים 0 (כדי לא לתקוע את התוכנית לנצח אם אין הד בכלל, למשל אין שום דבר מול החיישן).

### 2.3 חיישן צבע TCS3200 — זיהוי צבע שיער (`TCS3200_Color_sensor.ino`)

**תפקיד:** כשמשתמשת בוחרת אפשרות "כצבע שערי" (MyHair), החיישן סורק את צבע השיער שהוכנס ומתאים אותו לצבע התוספת (Blonde/Green/Pink/Black) הקרוב ביותר בקרוסלה.

**עקרון הפעולה הפיזי:** TCS3200 הוא חיישן שממיר עוצמת אור (דרך פילטרים צבעוניים פנימיים: אדום/ירוק/כחול/ללא-פילטר) ל**תדר** — כמות אור רבה יותר בערוץ מסוים = תדר גבוה יותר בפלט. הפינים S2/S3 בוחרים איזה פילטר צבע פעיל כרגע, S0/S1 קובעים "סקאלת תדר" (scaling) — כמה לחלק את התדר הגולמי כדי שיהיה נוח למדידה.

```cpp
void colorSensorSetup() {
  pinMode(S0, OUTPUT); pinMode(S1, OUTPUT);
  pinMode(S2, OUTPUT); pinMode(S3, OUTPUT);
  pinMode(COLOR_OUT, INPUT);        // כאן קוראים את התדר הנמדד
  digitalWrite(S0, HIGH);           // S0=HIGH, S1=LOW => לפי דף הנתונים = סקאלת תדר 20%
  digitalWrite(S1, LOW);
}
```

**קריאת ערוץ בודד:**
```cpp
static int readChannelRaw(int s2State, int s3State) {
  digitalWrite(S2, s2State);        // בוחר איזה פילטר צבע (אדום/ירוק/כחול) פעיל
  digitalWrite(S3, s3State);
  delay(50);                        // נותן לפלט "להתייצב" אחרי שינוי הפילטר
  return pulseIn(COLOR_OUT, LOW, 50000);  // מודד את משך פולס ה-LOW = פרופורציוני לתדר
}
```
טבלת שילובי S2/S3 (לפי דף הנתונים של TCS3200):
- אדום: S2=LOW, S3=LOW
- ירוק: S2=HIGH, S3=HIGH
- כחול: S2=LOW, S3=HIGH

```cpp
static long readChannelAvg(int s2State, int s3State) {
  long sum = 0;
  for (int i = 0; i < COLOR_SUBSAMPLES; i++)   // COLOR_SUBSAMPLES = 5 (ב-Config.h)
    sum += readChannelRaw(s2State, s3State);
  return sum / COLOR_SUBSAMPLES;    // ממוצע של 5 מדידות — מחליק רעש/תנודות
}
```

```cpp
static void readRGBonce(int &r, int &g, int &b) {
  long red   = readChannelAvg(LOW,  LOW);
  long green = readChannelAvg(HIGH, HIGH);
  long blue  = readChannelAvg(LOW,  HIGH);
  r = constrain(map(red,   RED_MIN,   RED_MAX,   255, 0), 0, 255);
  g = constrain(map(green, GREEN_MIN, GREEN_MAX, 255, 0), 0, 255);
  b = constrain(map(blue,  BLUE_MIN,  BLUE_MAX,  255, 0), 0, 255);
}
```
- `RED_MIN/RED_MAX` וכו' ב-`Config.h` הם **ערכי כיול** (calibration) — נמדדו מראש: מה התדר שהחיישן מודד כשהוא רואה משטח לבן לגמרי (תדר נמוך = הרבה אור) לעומת שחור לגמרי (תדר גבוה = מעט אור מוחזר).
- `map(red, RED_MIN, RED_MAX, 255, 0)` — ממיר את התדר הגולמי לסקאלה סטנדרטית 0-255. שימי לב שהטווח הפוך (`255, 0` ולא `0, 255`) כי ב-TCS3200 **תדר נמוך = יותר אור מוחזר = צבע בהיר יותר**, ולכן צריך להפוך את הכיוון כדי שהתוצאה תתאים לסקאלת RGB רגילה (0=כהה, 255=בהיר).
- `constrain(..., 0, 255)` — מוודא שהתוצאה לא חורגת מהטווח החוקי גם אם המדידה בפועל הייתה מעט מחוץ לטווח הכיול.

**מציאת הצבע הקרוב ביותר (מרחק אוקלידי בתלת-ממד RGB):**
```cpp
static const char* findClosestColor(int r, int g, int b) {
  long minDist = LONG_MAX;
  const char* best = "Unknown";
  for (int i = 0; i < numColors; i++) {
    long dr = r - colorRefs[i].r;
    long dg = g - colorRefs[i].g;
    long db = b - colorRefs[i].b;
    long dist = dr*dr + dg*dg + db*db;   // מרחק בריבוע (חוסך את חישוב השורש, לא נחוץ להשוואה)
    if (dist < minDist) { minDist = dist; best = colorRefs[i].name; }
  }
  if (minDist > MAX_COLOR_DIST) return "Unknown";  // אפילו הכי קרוב רחוק מדי -> לא אמין
  return best;
}
```
- זה בעצם חישוב **מרחק בין שתי נקודות במרחב תלת-ממדי** (R,G,B כצירים) — בדיוק כמו נוסחת פיתגורס אבל ל-3 מימדים: מרחק² = (Δr)² + (Δg)² + (Δb)².
- לא מחשבים שורש (sqrt) כי זה יקר חישובית ולא נחוץ — כדי להשוות "מי קרוב יותר" מספיק להשוות את הריבועים עצמם.
- `MAX_COLOR_DIST` (3000) הוא סף בטיחות: אם אפילו הצבע הכי קרוב עדיין רחוק מאוד (למשל בגלל תאורה חלשה שגורמת לכל הערוצים לצנוח לאפס), עדיף להגיד "Unknown" מאשר להתאים בכוח לצבע שגוי.

**3 דגימות → ממוצע → תוצאה סופית:**
```cpp
String readHairColor() {
  long sr = 0, sg = 0, sb = 0;
  for (int i = 0; i < COLOR_READINGS; i++) {   // COLOR_READINGS = 3
    int r, g, b; readRGBonce(r, g, b);
    sr += r; sg += g; sb += b;
  }
  int r = sr / COLOR_READINGS;
  int g = sg / COLOR_READINGS;
  int b = sb / COLOR_READINGS;
  return String(findClosestColor(r, g, b));
}
```
כלומר יש כאן **שתי שכבות ממוצע**: 5 מדידות גולמיות פנימיות לכל ערוץ צבע (`COLOR_SUBSAMPLES`), ועוד 3 קריאות RGB מלאות שמוצע ביניהן (`COLOR_READINGS`) — כדי לקבל תוצאה יציבה וללא רעש אקראי.

---

## 3. תקשורת בין שני הלוחות — פרוטוקול UART

זהו אחד החלקים החשובים ביותר להבין להגשה: **איך שני ה-ESP32 "מדברים"**.

### 3.1 מה זה UART ולמה בכלל צריך את זה

UART (Universal Asynchronous Receiver/Transmitter) הוא פרוטוקול תקשורת טורית פשוט — שני חוטים (TX=שידור, RX=קליטה) + GND משותף. כל צד שולח בייטים (תווים) אחד-אחד, בקצב מוסכם מראש (baud rate).

**חיווט (מ-`Config.h` בשני הלוחות):**
```
ראשון TX (פין 17) ---> שני RX (פין 16)
ראשון RX (פין 16) <--- שני TX (פין 17)
GND ראשון          <--> GND שני   (חובה! משותף לשני הלוחות)
```
זה **חיבור צולב** (crossover): TX של אחד מתחבר ל-RX של השני, כי מה שאחד "משדר" השני צריך "לקלוט".

שני הלוחות משתמשים ב-`Serial2` (UART2 הפנימי של ה-ESP32, לא זה שמחובר ל-USB) כדי לא להתנגש עם ה-USB שמשמש להעלאת קוד ולדיבאג בקונסולה (`Serial`/UART0).

```cpp
Serial2.begin(UART_BAUD, SERIAL_8N1, UART_RX_PIN, UART_TX_PIN);  // 115200 baud, 8 ביט נתונים, ללא parity, 1 stop bit
```

### 3.2 הפרוטוקול המלא (שורות טקסט שמסתיימות ב-`\n`)

מוגדר במפורש בראש `first_esp32.ino`:

| כיוון | פקודה | משמעות |
|---|---|---|
| שני → ראשון | `DIST?` | "תמדוד לי מרחק עכשיו" |
| ראשון → שני | `DIST:<cm>` ואז `DISTOK:<0/1>` | המרחק שנמדד + האם הוא בטווח התקין |
| שני → ראשון | `COLOR?` | "תסרוק לי צבע שיער" |
| ראשון → שני | `COLOR:<name>` | שם הצבע שזוהה (Blonde/Green/Pink/Black/Unknown) |
| שני → ראשון | `ARMED` | "אני מתחיל קליעה — תתחיל לנטר כפתור חירום" |
| שני → ראשון | `DISARM` | "סיימתי — תפסיק לנטר חירום" |
| ראשון → שני | `EMG` | "המשתמשת לחצה על הכפתור באמצע קליעה — עצור הכל!" |
| ראשון → שני | `OK` | "לחיצת אישור רגילה (לא חירום)" |
| שני → ראשון | `PING` | בדיקת קשר |
| ראשון → שני | `PONG` | תשובה לבדיקת הקשר |
| שני → ראשון | `DISP:<n>` | "סובבי את קרוסלת התוספות למקום n" (n=0..3, שלילי=בלי תוספת) |
| ראשון → שני | `DISPDONE` | "הקרוסלה הגיעה ליעד" |

### 3.3 קליטת שורה תו-אחר-תו — הלוגיקה בפועל

זו הפונקציה שרצה **כל הזמן** ב-`loop()` של הלוח הראשון, וקוראת מה שמגיע מה-UART:

```cpp
void handleUart() {
  while (Serial2.available()) {      // כל עוד יש בייטים ממתינים בבאפר הקליטה
    char c = Serial2.read();         // קורא בייט (תו) אחד
    if (c == '\n' || c == '\r') {    // תו סוף-שורה => השורה הושלמה
      if (rxLine.length() > 0) {
        processCommand(rxLine);      // מפענח ומבצע את הפקודה שהצטברה
        rxLine = "";                 // מאפס בופר לקראת השורה הבאה
      }
    } else {
      rxLine += c;                   // תו רגיל — מצרף לבופר המצטבר
      if (rxLine.length() > 40) rxLine = "";  // הגנה מפני "זבל" (למשל רעש בקו) שלא נגמר
    }
  }
}
```

**עקרון חשוב:** UART הוא זרם בייטים בלי מבנה מובנה — אין "התחלה"/"סוף" מוגדרים מראש. לכן הצד השולח **תמיד** מוסיף `\n` (ירידת שורה) בסוף כל הודעה (בפונקציית `sendLine`), והצד המקבל **צובר תווים לבאפר עד שהוא רואה `\n`**, ואז מפרש את כל מה שהצטבר כפקודה שלמה. זו טכניקה נפוצה מאוד לתקשורת טורית מבוססת-טקסט.

**שליחה (הצד השני של אותה מטבע):**
```cpp
void sendLine(const String& s) {
  Serial2.print(s);     // תוכן ההודעה עצמה
  Serial2.print('\n');  // תו סיום שורה — "סימן" שהמשפט נגמר
}
```

**עיבוד הפקודה שהתקבלה:**
```cpp
void processCommand(const String& cmd) {
  if (cmd == "DIST?") {
    float d = readDistanceCm();                        // קורא בפועל מהחיישן
    bool ok = (d >= DIST_MIN_CM && d <= DIST_MAX_CM);
    sendLine("DIST:" + String(d, 1));                  // שולח את המרחק (ספרה עשרונית אחת)
    sendLine("DISTOK:" + String(ok ? 1 : 0));           // שולח אם זה תקין (0/1)
  }
  else if (cmd == "COLOR?") {
    String name = readHairColor();
    sendLine("COLOR:" + name);
  }
  else if (cmd == "ARMED")  { armed = true;  lastBtnPressed = false; }
  else if (cmd == "DISARM") { armed = false; }
  else if (cmd == "PING")   { sendLine("PONG"); }
  else if (cmd.startsWith("DISP:")) {
    int idx = cmd.substring(5).toInt();   // חותך את המחרוזת אחרי "DISP:" והופך למספר
    dispenserGoTo(idx);
    sendLine("DISPDONE");
  }
}
```
כל `if/else` פשוט משווה את הפקודה שהתקבלה למחרוזת ידועה מראש ומריץ את הפעולה המתאימה. `cmd.startsWith("DISP:")` בודק אם המחרוזת **מתחילה** ב"DISP:" (כי אחריה מגיע מספר משתנה), ו-`cmd.substring(5)` חותך את 5 התווים הראשונים ("DISP:") ומשאיר רק את המספר, ש-`.toInt()` הופך למספר שלם.

### 3.4 הצד השני — הלוח השני **מבקש** מידע ומחכה (`UartLink.ino`)

הלוח השני לא רק שולח פקודות — הוא גם **ממתין בסבלנות לתשובה**, עם timeout כדי לא להיתקע לנצח אם אין תגובה:

```cpp
bool requestDistanceOk(uint32_t timeoutMs) {
  lastDistanceCm = -1;               // מאפס לפני הבקשה, כדי לזהות שהגיעה תשובה חדשה
  uartSend("DIST?");                 // שולח את הבקשה
  uint32_t t0 = millis();
  while (millis() - t0 < timeoutMs) {  // מחכה עד timeoutMs מילישניות
    uartPoll();                      // בודק אם הגיעו בייטים חדשים ומעדכן משתנים גלובליים
    if (lastDistanceCm >= 0) {                   // תשובה כבר התקבלה!
      return lastDistOk;
    }
  }
  return false;                      // פסק זמן — לא קיבלנו תשובה בזמן
}
```

- זהו תבנית עיצוב נפוצה מאוד: **"שלח בקשה, המתן בלולאה עם timeout, בדוק דגל גלובלי שמתעדכן ברקע"**.
- `uartPoll()` היא הפונקציה שבפועל קוראת את ה-UART ומעדכנת משתנים (`lastDistanceCm`, `lastColor`, `emergencyRequested` וכו') בכל פעם שמגיעה שורה שלמה — היא נקראת גם כאן בלולאת ההמתנה, וגם ממקומות אחרים בקוד (למשל בזמן קליעה, כדי לא לפספס הודעת חירום שמגיעה תוך כדי).

### 3.5 הודעת החירום — הדוגמה הכי קריטית לזרימת המידע

1. משתמשת לוחצת על הכפתור הפיזי המחובר ל**לוח הראשון**.
2. `handleButton()` בלוח הראשון מזהה לחיצה חדשה, ורואה ש-`armed == true` (כי הלוח השני שלח `ARMED` כשהקליעה התחילה).
3. הלוח הראשון שולח `sendLine("EMG")` דרך ה-UART.
4. בלוח השני, `parseLine()` ב-`UartLink.ino` קולט את המחרוזת `"EMG"` ומעדכן `emergencyRequested = true` (משתנה מסוג `volatile bool` — גלובלי, נגיש מכל מקום בקוד).
5. בתוך `braidWhileLowering()` ב-`Motors.ino`, כל כמה צעדים נקרא `uartPoll()` ונבדק `if (emergencyRequested)` — אם כן, שני המנועים נעצרים **מיידית** (`releaseCoils`) והפונקציה מחזירה `false`.
6. חזרה ב-`second_esp32.ino`, ה-`runSession()` רואה `finished == false` ועובר למסלול הטיפול בחירום: `motorsStopAll()`, מסך אדום (`uiEmergency()`), שמירת ההזמנה בסטטוס `"emergency"` ל-Firebase.

זו הדגמה מושלמת של **תקשורת דו-כיוונית בזמן אמת** בין שני מיקרו-בקרים דרך UART.

---

## 4. תקשורת עם הענן — WiFi + Firebase (Cloud Firestore)

הלוח השני בלבד מתחבר לאינטרנט (הלוח הראשון "מבודד" — מדבר רק עם הלוח השני דרך UART).

### 4.1 חיבור ל-WiFi (`FirebaseManager.ino`, פונקציית `firebaseSetup`)

```cpp
WiFi.begin(WIFI_SSID, WIFI_PASSWORD);         // מתחיל ניסיון חיבור
uint32_t t0 = millis();
while (WiFi.status() != WL_CONNECTED && millis() - t0 < 10000) delay(300);  // מחכה עד 10 שניות
```
לולאת המתנה עם timeout — זהה בעיקרון ללולאות ה-UART שראינו למעלה. `WiFi.status()` מחזיר קבוע (`WL_CONNECTED` אם התחברנו בהצלחה).

### 4.2 התחברות ל-Firebase כ"מכשיר"

```cpp
fbAuth.user.email    = FB_DEVICE_EMAIL;   // "device@braid.machine"
fbAuth.user.password = FB_DEVICE_PASS;
Firebase.begin(&fbConfig, &fbAuth);
```
ה-ESP32 מתחבר ל-Firebase **כאילו הוא משתמש** עם אימייל/סיסמה קבועים מראש (לא הלקוחה עצמה!) — זה נדרש כי חוקי האבטחה (Security Rules) של Firestore דורשים אימות (authentication) כדי לקרוא/לכתוב נתונים. יש גם מנגנון גיבוי: אם הכניסה נכשלת (כי החשבון עוד לא נוצר בכלל), הקוד מנסה ליצור אותו אוטומטית עם `Firebase.signUp(...)`.

**הערה קריטית מהקוד עצמו (הערת אזהרה שהתגלתה מניסיון בפועל):** אסור לבדוק `Firebase.ready()` בלולאה צפופה (למשל כל 200 מ"ש) — זה שולח יותר מדי בקשות אימות לגוגל ברצף, וגוגל חוסמת זמנית עם שגיאת `TOO_MANY_ATTEMPTS_TRY_LATER`. הפתרון שנבחר: קריאה אחת ל-`begin()`, המתנה קבועה (`delay(4000)`) בלי בדיקה בלולאה, ואז בדיקה **בודדת** של `ready()`.

### 4.3 מבנה הנתונים ב-Firestore

```
codes/{code}   = { uid, name, used, createdAt }     // קוד זמני בן 4 ספרות שהלקוחה מקבלת
orders/{id}    = { uid, name, extensions, hairColor, status, createdAt }  // הזמנה שבוצעה/נעצרה
```

**אימות קוד (`fbValidateCode`):**
```cpp
Firebase.Firestore.getDocument(&fbdo, FIREBASE_PROJECT_ID, "", ("codes/" + code).c_str(), "");
FirebaseJson doc;
doc.setJsonData(fbdo.payload());       // ממיר את התשובה הגולמית (JSON) לאובייקט נוח לשליפה
name = fsStr(doc, "name");             // שולף שדה טקסט מתוך מבנה ה-JSON של Firestore
uid  = fsStr(doc, "uid");
bool used = fsBool(doc, "used");
if (used) return false;                // קוד כבר נוצל בעבר -> נדחה
```
`Firestore.getDocument` היא קריאת HTTPS ל-Google (Firebase REST API), עוטפת בתוך הספרייה `Firebase_ESP_Client`. Firestore מחזיר JSON במבנה מיוחד (`fields/<name>/stringValue` וכו') ולכן יש פונקציות עזר `fsStr`/`fsBool` שמחלצות את הערך הנקי מתוך המבנה המקונן הזה.

**שמירת הזמנה (`fbSaveOrder`)** בונה מסמך JSON חדש עם `FirebaseJson json; json.set("fields/uid/stringValue", uid); ...` ושולח אותו ב-`Firebase.Firestore.createDocument(...)` תחת `orders/{orderId}`, כאשר ה-`orderId` נבנה מ-`uid + "_" + millis()` (כדי שיהיה ייחודי בלי צורך במסד נתונים לספור IDs).

### 4.4 מצב "דמו" — עבודה בלי אינטרנט בכלל

אם ה-WiFi לא מוגדר (`WIFI_SSID == "YOUR_WIFI"`) או שההתחברות נכשלת, כל הפונקציות עוברות למצב fallback:
```cpp
if (!fbReady) {
  name = "Guest"; uid = "demo";
  bool ok = (code.length() == CODE_LENGTH);   // כל קוד בן 4 ספרות מתקבל
  return ok;
}
```
זה מאפשר להדגים את כל המכונה (חיישנים, מנועים, מסך) **גם בלי חיבור אינטרנט בכלל** — שימושי מאוד להדגמה בכיתה/הגשה.

---

## 5. גרסת `esp32_firebase` — לוח יחיד עם שרת HTTP ל-React (לא בשימוש הפעיל)

תיקייה זו מכילה גרסה שונה, ישנה יותר: **לוח ESP32 יחיד** (בלי לוח שני, ובלי UART) שמריץ בעצמו שרת HTTP (`WebServer.ino`, פורט 80) כדי לתקשר עם אפליקציית ה-React (`react-app/`) ישירות דרך הרשת המקומית, במקום עם מסך מגע פיזי.

- `webServerBegin()` רושם נתיבים (`/login`, `/register`, `/appointment`, `/start`, `/stop` וכו') — כל אחד מקביל לפעולה שה-React שולח כ-בקשת HTTP (POST/GET).
- כל תגובה כוללת כותרות CORS (`Access-Control-Allow-Origin: *`) כדי שדפדפן ירשה לאתר React (שרץ על כתובת/פורט אחר) לקרוא לשרת שרץ על ה-ESP32.
- `SensorManager.ino` בגרסה זו **ריק** ("לא בשימוש בגרסה זו") — כלומר בגרסה הזו לא היו עדיין חיישנים מחוברים בפועל; הפוקוס היה על זרימת המשתמש/Firebase/מסך. **כל לוגיקת החיישנים האמיתית נמצאת רק ב-`first_esp32/`**, שהיא הגרסה הנוכחית/הפעילה של הפרויקט.

חשוב לציין את זה בהגשה כדי שלא יהיה בלבול בין שתי הארכיטקטורות (`esp32_firebase` הישן מול הזוג `first_esp32`+`second_esp32` הנוכחי).

---

## 6. איך זורם תהליך מלא (End-to-End) במערכת הנוכחית

1. **אתחול (`setup()`):** שני הלוחות עולים, כל אחד מאתחל את הרכיבים שלו (חיישנים בראשון; מסך/מנועים/WiFi בשני). הלוח השני מבקש מהראשון להחזיר את קרוסלת התוספות למקום 0 (`requestDispenserGoTo(0, ...)`).
2. **הקלדת קוד:** הלקוחה מקלידה קוד בן 4 ספרות במסך המגע (`uiGetCode`), הלוח השני שולח אותו ל-Firestore לאימות (`fbValidateCode`) ומקבל בחזרה שם ומזהה משתמש.
3. **בחירת תוספות:** מסך בחירה (`uiSelectExtensions`) — עד 3 תוספות, כולל אופציה "כצבע שערי".
4. **הכנסת שיער + START:** מסך הוראה (`uiInsertHair`).
5. **בדיקת מרחק:** הלוח השני שולח `DIST?` ללוח הראשון וממתין לתשובה מהחיישן האולטרסוני; חוזר על הבדיקה עד שהמרחק תקין.
6. **זיהוי צבע (אם נבחר "כצבע שערי"):** הלוח השני שולח `COLOR?`, הלוח הראשון סורק עם TCS3200 ומחזיר שם צבע.
7. **הגשת תוספות:** לכל תוספת שנבחרה, הלוח השני שולח `DISP:<n>` והלוח הראשון מסובב את הקרוסלה למקום המתאים.
8. **אישור סופי לפני קליעה (`uiReadyToBraid`).**
9. **קליעה:** הלוח השני שולח `ARMED` (מפעיל ניטור חירום בראשון), ואז מריץ בו-זמנית את מנוע המסילה (יורד) ומנוע הקליעה (מסתובב), תוך קריאה תכופה ל-`uartPoll()` כדי לתפוס הודעת `EMG` מיידית אם מגיעה. בסיום שולח `DISARM`.
10. **סיום תקין / חירום:** בהתאם לתוצאה, מוצג מסך ירוק ("DONE") או אדום ("EMERGENCY"), המסילה חוזרת למעלה, וההזמנה נשמרת ב-Firestore (`orders/{id}`) עם סטטוס `"completed"` או `"emergency"`.

---

## 7. מכונת המצבים (State Machine) של הלוח השני

הפונקציה `runSession()` ב-`second_esp32.ino` **היא** מכונת המצבים המרכזית של כל המערכת (וכך גם כתוב בהערת התיעוד בראש הקובץ). זו לא מכונת מצבים "קלאסית" עם `switch(state)` ומשתנה `state` מפורש — היא כתובה כ**רצף לינארי חוסם** (blocking sequence): כל שלב הוא פונקציה שמחכה בלולאה (`while`) עד שהתנאי לצאת ממנה מתקיים, ורק אז עוברים לשלב הבא. זו עדיין מכונת מצבים לכל דבר — פשוט המצב הנוכחי הוא "היכן אנחנו נמצאים בקוד" במקום משתנה נפרד.

### תרשים המצבים

```
[התחלה]
   │
   ▼
┌─────────────────────┐  קוד שגוי/לא קיים
│ S1: המתנה לקוד       │◄────────────────────┐
│ uiGetCode()          │                      │
│ fbValidateCode()     │──────────────────────┘
└─────────┬────────────┘  קוד תקין (name, uid ידועים)
          ▼
┌─────────────────────┐
│ S2: בחירת תוספות     │
│ uiSelectExtensions() │  (עד 3 תוספות, כולל "MyHair")
└─────────┬────────────┘
          ▼
┌─────────────────────┐
│ S3: הכנסת שיער       │
│ uiInsertHair()       │  (ממתין ללחיצת START)
└─────────┬────────────┘
          ▼
┌─────────────────────┐  מרחק לא תקין
│ S4: בדיקת מרחק       │◄────────────────────┐
│ requestDistanceOk()  │──────────────────────┘
└─────────┬────────────┘  מרחק תקין
          ▼
┌─────────────────────┐
│ S5: זיהוי צבע        │  (רק אם נבחר MyHair)
│ requestHairColor()   │
└─────────┬────────────┘
          ▼
┌─────────────────────┐
│ S6: הגשת תוספות      │
│ requestDispenserGoTo()│ (לכל תוספת: מסובב קרוסלה + מחכה שהמשתמשת תיקח)
│ uiTakeExtension()    │
└─────────┬────────────┘
          ▼
┌─────────────────────┐
│ S7: אישור לקליעה     │
│ uiReadyToBraid()     │  (ממתין ללחיצת START נוספת ומודעת — "נקודת אין חזרה")
└─────────┬────────────┘
          ▼
┌─────────────────────┐
│ S8: קליעה פעילה      │  ◄── כאן בלבד ARMED (מנטרים כפתור חירום)
│ braidWhileLowering() │
└──────┬───────┬───────┘
       │        │
  finished=true │ finished=false (EMG התקבל)
       │        │
       ▼        ▼
┌───────────┐ ┌────────────────────┐
│S9: סיום   │ │S8b: טיפול חירום     │
│uiDone()   │ │motorsStopAll()      │
│railUpHome │ │uiEmergency()        │
│fbSaveOrder│ │railUpHome()         │
│("completed")│fbSaveOrder("emergency")│
└─────┬─────┘ └──────────┬──────────┘
      │                  │
      └────────┬─────────┘
               ▼
        [חזרה ל-S1 — לולאה אינסופית ב-loop()]
```

### נקודות מפתח על מכונת המצבים

- **המצב "הנוכחי" נקבע ע"י `program counter`** — כלומר איפה בקוד אנחנו רצים כרגע, לא ע"י משתנה `int state` מפורש. `loop()` פשוט קורא שוב ושוב ל-`runSession()` מההתחלה, וכל קריאה = מחזור מלא אחד (session) של לקוחה אחת מקוד ועד שמירת ההזמנה.
- **המעברים בין מצבים הם חסימתיים (blocking)** — כל פונקציית `ui...()` היא בעצמה לולאת `while(true)` פנימית שמחכה למגע/UART ולא חוזרת עד שהתנאי ליציאה מתקיים. זה אומר שאין "ריבוי משימות" אמיתי — המכונה עושה דבר אחד בכל רגע נתון, לפי הסדר. זה מתאים מאוד למכונה פיזית שבה השלבים באמת חייבים לקרות בזה אחר זה (אי אפשר לקלוע לפני שנבדק מרחק, למשל).
- **המצב היחיד עם ניטור מקבילי אמיתי הוא S8 (קליעה)** — שם בתוך `braidWhileLowering()` יש `while` שגם מזיז מנועים **וגם** קורא `uartPoll()` בכל איטרציה, כדי לתפוס הודעת `EMG` "תוך כדי תנועה" ולא לפספס אותה. זו הסיבה שדווקא השלב הזה "יכול להיקטע" (S8→S8b), בעוד ששאר השלבים פשוט ממתינים ללחיצה/תשובה בלי אפשרות הפרעה.
- **`ARMED`/`DISARM` הם בעצם "כניסה למצב" ו"יציאה ממצב" עבור הלוח הראשון** — הלוח הראשון עצמו מחזיק משתנה `armed` (ראי `first_esp32.ino`) שמייצג אם *הוא* נמצא במצב "מנטר חירום" — זו בעצם תת-מכונת-מצבים קטנה בלוח השני שמשקפת (מרחוק, ב-UART) את מצב הלוח הראשון.
- **אין "state" גלובלי שאפשר לשאול עליו מבחוץ** — בניגוד למשל ל-`gActiveAppt.active` בקובץ `esp32_firebase/AppointmentManager.ino` (הגרסה הישנה), שם יש משתנה בוליאני מפורש שאפשר לשאול אותו מכל מקום. בגרסה הנוכחית (`second_esp32`) המצב "חי" רק בתוך ה-stack call של `runSession()` בזמן שהוא רץ.

---

## 8. נקודות שכדאי לדעת "בעל פה" למקרה שישאלו בהגשה

- **מדוע יש שני לוחות ולא אחד?** מגבלת פינים (GPIO) — למסך TFT יש דרישות פינים קבועות (SPI), וביחד עם 2 מנועי צעד (8 פינים) ומגע כבר אין מספיק פינים פנויים ללוח היחיד גם לחיישנים וגם למנוע התוספות. לכן פוצל לשני לוחות שמדברים ב-UART.
- **למה UART ולא WiFi בין הלוחות?** UART מקומי, מהיר, פשוט וללא תלות ברשת אלחוטית — מתאים לתקשורת קרובה בין שני רכיבים על אותו מכשיר.
- **למה יש דיבאונס לכפתור?** למנוע קריאה כפולה בגלל "רעש" מכני בזמן הלחיצה/שחרור הפיזי של המתג.
- **למה מודדים ממוצע של כמה דגימות בחיישן הצבע ולא דגימה בודדת?** כדי להתגבר על רעש/תנודות אקראיות במדידה בודדת ולקבל תוצאה יציבה יותר.
- **למה יש `MAX_COLOR_DIST` ("Unknown")?** כדי לא "להכריח" התאמה לצבע שגוי כשהמדידה לא אמינה (למשל תאורה חלשה).
- **מה קורה אם אין אינטרנט בכלל?** המערכת עדיין עובדת במלואה במצב "דמו" (קוד "1111" תמיד מתקבל, שם="Guest"), רק בלי שמירה בפועל בענן.
- **מהי משמעות `volatile` על `emergencyRequested`?** מסמן לקומפיילר שהערך יכול להשתנות "מבחוץ" (כאן: בתוך `uartPoll()` שנקרא מלולאת קוד אחרת) ולכן אסור לו "לשמור בזיכרון מטמון/רגיסטר" ולדלג על קריאה טרייה מהזיכרון בכל פעם.

---

## 9. מנועי הצעד (Stepper Motors) — איך הם באמת זזים

בפרויקט יש **3 מנועי צעד** בסך הכל: מנוע קרוסלת התוספות (בלוח הראשון), ומנוע המסילה + מנוע הקליעה (שניהם בלוח השני). כולם מנוע `28BYJ-48` (מנוע צעד יוניפולרי חלש וזול, נפוץ מאוד בפרויקטי לימוד) שמופעל דרך דרייבר `ULN2003`, **חוץ** ממנוע המסילה שמופעל דרך דרייבר `L298N` (H-bridge בי-פולרי). זה ההבדל החשוב ביותר להבין כאן.

### 9.1 העיקרון הבסיסי — מה זה בכלל "פאזה"

מנוע צעד לא מסתובב ברציפות כמו מנוע DC רגיל — יש לו כמה סלילים (coils) פנימיים, וכדי להזיז אותו "צעד" אחד קדימה צריך להדליק/לכבות אותם ב**רצף מדויק**. כל שילוב הדלקה כזה נקרא "פאזה". אם עוברים על 4 הפאזות ברצף שוב ושוב, הציר מסתובב ברציפות; אם עוברים עליהן בסדר הפוך, הוא מסתובב לכיוון השני.

### 9.2 מנוע יוניפולרי (ULN2003) — קליעה + תוספות

```cpp
static const uint8_t PHASE_SEQ[4][4] = {
  {1,1,0,0},   // פאזה 0: סלילים 1+2 דלוקים
  {0,1,1,0},   // פאזה 1: סלילים 2+3 דלוקים
  {0,0,1,1},   // פאזה 2: סלילים 3+4 דלוקים
  {1,0,0,1}    // פאזה 3: סלילים 4+1 דלוקים
};
```
זו טבלה בת 4 שורות (פאזות) על 4 עמודות (פינים IN1..IN4). בכל פאזה **שני** פינים סמוכים דלוקים בו-זמנית ("half-step" דו-סלילי) — זה נותן מומנט (כוח) גבוה יותר מאשר להדליק סליל בודד כל פעם, במחיר צריכת חשמל מעט גבוהה יותר. מכיוון שזה מנוע יוניפולרי, כל פין הוא פשוט HIGH/LOW רגיל (1=דלוק, 0=כבוי) — אין צורך בכיוון זרם הפוך.

```cpp
static void stepOnce(const int p[4], int &phase, int dir) {
  phase = (phase + dir + 4) % 4;   // מקדם/מוריד את הפאזה במעגליות (0→1→2→3→0...)
  applyPhase(p, phase);            // מדליק את הפינים לפי הפאזה החדשה
  delay(STEP_DELAY_MS);            // משהה כדי לתת לרוטור זמן "לתפוס" לפני הצעד הבא
}
```
- `(phase + dir + 4) % 4` — זו הדרך הנקייה לבצע "חשבון מעגלי" (modular arithmetic): `dir` הוא `+1` או `-1`. בלי ה-`+4`, אם `phase=0` ו-`dir=-1` היינו מקבלים `-1 % 4` שבשפת C++ יכול לתת תוצאה שלילית (`-1`) במקום `3` — לכן מוסיפים 4 מראש כדי להבטיח שהתוצאה תמיד חיובית לפני ה-modulo.
- `STEP_DELAY_MS` קובע את **המהירות**: השהיה קצרה = צעדים מהירים = סיבוב מהיר יותר, אבל אם קצר מדי הרוטור "לא מספיק לתפוס" מגנטית ומתחיל לרעוד במקום להסתובב (מומנט אפקטיבי נמוך) — יש על כך הערה מפורשת ב-`second_esp32/Config.h:46-47` (הועלה מ-3 ל-8 מ"ש בגלל זה בדיוק).

**קרוסלת התוספות** (`first_esp32/Dispenser.ino`) עובדת עם אותה טבלת פאזות בדיוק, אבל בתנועה של "רבע סיבוב" בין כל מקום (`DISP_QUARTER_STEPS = DISP_STEPS_PER_REV / 4`, כלומר `2048/4 = 512` צעדים) — כי יש 4 מקומות שווים בקרוסלה (Blonde/Green/Pink/Black), אז מעבר בין מקום למקום = רבע סיבוב מלא בדיוק.

```cpp
void dispenserGoTo(int targetIndex) {
  int diff = targetIndex - dispIndex;    // כמה "מקומות" (לא צעדים!) להזיז, יכול להיות שלילי
  int dir  = (diff >= 0) ? 1 : -1;
  int moves = abs(diff);
  for (int m = 0; m < moves; m++)
    for (int s = 0; s < DISP_QUARTER_STEPS; s++)
      stepOnce(dir);                     // רבע סיבוב שלם לכל "מקום" שצריך לעבור
  dispIndex = targetIndex;               // מעדכן איפה הקרוסלה נמצאת עכשיו
}
```
שימי לב שהמנוע **זוכר את מיקומו הנוכחי** (`dispIndex`) בזיכרון בלבד (משתנה `static int`) — אין חיישן מיקום (כמו אנקודר) שמאשר בפועל שהקרוסלה הגיעה ליעד. זו נקודת תורפה אפשרית: אם המנוע "מפספס צעד" פיזית (למשל בגלל עומס יתר), התוכנה לא תדע שהמיקום האמיתי שונה מהמיקום שהיא חושבת שהוא נמצא בו — שווה לציין את זה כמגבלה ידועה אם שואלים על שיפורים אפשריים.

### 9.3 מנוע בי-פולרי (L298N) — המסילה, וההבדל הקריטי

```cpp
static const uint8_t RAIL_PHASE_SEQ[4][4] = {
  {1,0,1,0},   // פאזה 0: A+ , B+
  {0,1,1,0},   // פאזה 1: A- , B+
  {0,1,0,1},   // פאזה 2: A- , B-
  {1,0,0,1}    // פאזה 3: A+ , B-
};
```
מנוע המסילה **מחובר דרך L298N** — דרייבר H-bridge בי-פולרי, לא ULN2003. בבי-פולרי, כל סליל (יש רק 2 סלילים, A ו-B) נשלט ע"י **זוג** פינים שקובע את **כיוון הזרם** דרכו (לא רק "דלוק/כבוי"): `IN1,IN2` שולטים על סליל A, `IN3,IN4` שולטים על סליל B. הערך `1` בטבלה לא אומר "החזק את הסליל דלוק" אלא "הזרם זורם בכיוון X"; `01` באותו זוג = כיוון הפוך; אבל **`1,1`** על אותו סליל (ב-L298N ספציפית) הוא מצב **בלימה** (braking) — לא הדלקה כפולה! זו הסיבה שיש הערת אזהרה מפורשת בקוד:

```cpp
// ⚠️ טבלה שונה לגמרי מ-PHASE_SEQ! "1,1" על אותו סליל ב-L298N = בלימה, לא הדלקה.
```

כלומר: **אסור** להשתמש באותה טבלת `PHASE_SEQ` גם למנוע הזה — זו טעות נפוצה כי שני המנועים "נראים" דומים (שניהם 28BYJ-48, שניהם 4 פינים), אבל סוג הדרייבר (ULN2003 מול L298N) מחייב טבלת פאזות שונה לגמרי.

### 9.4 המסילה במקטעים (Segments) — למה זה נחוץ

```cpp
#define RAIL_SEGMENT_STEPS 400
#define RAIL_MAX_SEGMENTS  7
```
המסילה לא יורדת בפעם אחת ב-`for` ארוך אחד — היא יורדת ב"מנות" של 400 צעדים (`RAIL_SEGMENT_STEPS`), עד 7 מנות (`RAIL_MAX_SEGMENTS`) = כל אורך המסילה. הסיבה: בכל מנה כזו צריך **גם** לבדוק אם הגיעה הודעת חירום (`uartPoll()`) **וגם** להזיז את מנוע הקליעה בו-זמנית (בתוך `braidWhileLowering()`). לכן הלולאה מפצלת את התנועה לחתיכות קטנות במקום להריץ 2800 צעדים ברצף בלי לבדוק כלום ביניהם:

```cpp
for (int b = 0; b < CHUNK; b++) stepOnce(braidPins, braidPhase, +1);   // 8 צעדי קליעה
for (int r = 0; r < CHUNK && doneInSegment < RAIL_SEGMENT_STEPS; r++, doneInSegment++)
  railStepOnce(+1);                                                     // עד 8 צעדי מסילה
```
`CHUNK=8` — כל איטרציה של הלולאה הפנימית מזיזה את שני המנועים 8 צעדים כל אחד, ואז חוזרים לבדוק `emergencyRequested` שוב. זו למעשה **הדמיית ריבוי-משימות** (cooperative multitasking) בלי מערכת הפעלה אמיתית — פשוט על ידי חיתוך העבודה לחתיכות קטנות ובדיקת תנאי עצירה בין כל חתיכה.

`railSegmentsDone` נשמר בדיוק כדי שאם קרה חירום **באמצע** מקטע כלשהו (למשל אחרי 3 מקטעים שלמים מתוך 7), כשחוזרים למעלה (`railUpHome()`) יעלו בדיוק `3 * 400 = 1200` צעדים ולא את המקסימום המלא — אחרת המסילה "תעלה יותר מדי" ותנסה לצאת מעבר לנקודת ההתחלה הפיזית שלה.

---

## 10. סקיצות הבדיקה (Test Sketches) — לצורך פיתוח בלבד

שלוש תיקיות קטנות נוספות בשורש הפרויקט אינן חלק מהמערכת הסופית, אלא שימשו לאבחון תקלות חומרה בזמן הפיתוח:

- **`pin_test/pin_test.ino`** — בודק פין GPIO בודד: כותב עליו HIGH ואז מיד קורא אותו בחזרה (`pinMode(TEST_PIN, INPUT)` זמנית) כדי לוודא שהוא "תופס" את הערך שנכתב. שימושי כדי לגלות פינים תקולים או פינים "מיוחדים" בESP32 שלא ניתן להשתמש בהם כרגיל (למשל פינים שמוקצים ל-flash הפנימי).
- **`uart_hello_test/uart_hello_test.ino`** — בדיקת תקשורת מינימלית בין שני הלוחות: כל לוח שולח `"HELLO from BOARD X"` פעם בשנייה ומדפיס כל מה שהוא מקבל. זו בדיוק אותה טכניקת "צבירת תווים עד `\n`" שראינו ב-`handleUart()`/`uartPoll()`, אבל בלי שום לוגיקה עסקית — נועד לוודא **רק** שהחיווט הפיזי (TX↔RX צולב, GND משותף, אותו baud rate) תקין לפני שמתחילים לדבג את הפרויקט המלא.
- **`esp32_board_test/esp32_board_test.ino`** — בדיקת לוח כללית (למשל ודא שה-ESP32 עצמו עולה ומגיב לפני שמחברים חיישנים/מנועים).

שווה להזכיר בהגשה שהשימוש בסקיצות בדיקה מבודדות כאלה הוא **שיטת עבודה טובה** בפיתוח חומרה: כשמשהו לא עובד במערכת המלאה, קשה לדעת אם הבעיה בחיווט, בפין ספציפי, בספרייה, או בלוגיקה — ולכן מבודדים כל רכיב לבדיקה עצמאית משלו לפני שמחברים הכל יחד.
