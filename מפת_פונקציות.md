# מפת פונקציות — כל הפונקציות בפרויקט, מה כל אחת אחראית, וזרימת הלוגיקה

מסודר לפי לוח וקובץ. המערכת הפעילה היא **first_esp32 + second_esp32** (דו-לוחית, UART).
`esp32_firebase` הוא ארכיטקטורה ישנה/חלופית (לוח יחיד + HTTP server), ותיקיות ה-`*_test` הן סקיצות בדיקה בלבד.

---

## 🔵 first_esp32 — לוח החיישנים

### first_esp32.ino (הקובץ הראשי)
| פונקציה | אחראית על |
|---|---|
| `setup()` | מאתחל Serial (דיבאג), Serial2 (UART ללוח השני), וכל תתי-המערכות: כפתור, אולטרסוני, צבע, דיספנסר |
| `loop()` | הלולאה הראשית — קוראת שוב ושוב ל-`handleUart()` ו-`handleButton()`, בלי לחסום |
| `handleUart()` | קוראת בתים מ-Serial2 תו-תו, בונה שורה עד `\n`/`\r`, ומעבירה כל שורה שלמה ל-`processCommand()` |
| `processCommand(cmd)` | "המפצל" של הפרוטוקול — לפי תוכן השורה שהתקבלה (`DIST?`, `COLOR?`, `ARMED`, `DISARM`, `PING`, `DISP:<n>`) מפעילה את הפעולה המתאימה ושולחת תשובה |
| `handleButton()` | קוראת את מצב הכפתור מ-`buttonPressed()`, מזהה רגע לחיצה חדש (עם דיבאונס), ולפי `armed` שולחת `EMG` (חירום) או `OK` (אישור) ללוח השני |
| `sendLine(s)` | שולחת מחרוזת + `\n` דרך Serial2 ללוח השני (הפונקציה הבסיסית לשליחה) |

**זרימת לוגיקה (loop):**
1. `handleUart()` — קוראת מה שהגיע מהלוח השני, ואם התקבלה שורה שלמה (בקשה כמו `DIST?`) — `processCommand()` מבצעת בפועל (למשל קוראת מרחק) ושולחת תשובה (`DIST:12.3` + `DISTOK:1`).
2. `handleButton()` — בודקת אם הכפתור הפיזי נלחץ ברגע הזה. אם כן ו-`armed=true` (המערכת "חמושה" לניטור חירום, הוגדר ע"י `ARMED` שהתקבל קודם) — שולחת `EMG`. אם לא armed (מצב רגיל) — שולחת `OK` (אישור/ניקוי).
3. חוזר חלילה בלי `delay()` — כדי לא לפספס תו UART או לחיצת כפתור.

### buttom_switch.ino
| פונקציה | אחראית על |
|---|---|
| `buttonSetup()` | מגדירה את פין הכפתור כ-`INPUT_PULLUP` |
| `buttonPressed()` | קוראת את מצב הפין הפיזי ומחזירה `true`/`false` אם הכפתור לחוץ כרגע (לפני דיבאונס — הדיבאונס נעשה ב-`handleButton()`) |

### Ultrasonic_esp32.ino
| פונקציה | אחראית על |
|---|---|
| `ultrasonicSetup()` | מגדירה פיני TRIG (פלט) ו-ECHO (קלט) של ה-HC-SR04 |
| `readDistanceCm()` | שולחת פולס TRIG, מודדת זמן החזרה עם `pulseIn()`, ומחשבת מרחק בס"מ לפי נוסחת מהירות הקול (`duration * 0.0343 / 2.0`) |

**זרימת לוגיקה:** `processCommand("DIST?")` קוראת ל-`readDistanceCm()` פעם אחת, משווה לטווח `DIST_MIN_CM..DIST_MAX_CM`, ושולחת בחזרה `DIST:<cm>` ואז `DISTOK:0/1`.

### TCS34725_Color_sensor.ino (חיישן צבע I2C — מחליף את ה-TCS3200 הישן)
| פונקציה | אחראית על |
|---|---|
| `colorSensorSetup()` | מאתחלת I2C (`Wire.begin`) על פיני `COLOR_SDA`/`COLOR_SCL`, ומפעילה את החיישן (`tcs.begin()`); מדפיסה שגיאה אם לא נמצא |
| `findClosestColor(r,g,b,c)` | משווה את הערכים הגולמיים (RAW R,G,B,Clear) שנמדדו למול טבלת `colorRefs` (4 צבעים מכוילים), במרחק אוקלידי בריבוע ב-4 מימדים; מחזירה את שם הצבע הקרוב ביותר, או `"Unknown"` אם המרחק המינימלי חורג מ-`MAX_COLOR_DIST` |
| `readHairColor()` | **דגימה בודדת** (לא ממוצע!) — קוראת RAW ישירות מהחיישן (`tcs.getRawData`) ומעבירה ל-`findClosestColor()`; מחזירה את שם הצבע כ-`String` |

**זרימת לוגיקה:** `processCommand("COLOR?")` קוראת ל-`readHairColor()` פעם אחת (דגימה בודדת RAW), ושולחת בחזרה `COLOR:<name>`.
> הערה: עובד לפי RAW (כולל ערוץ Clear) ולא RGB מנורמל — כי נרמול לפי Clear מוחק בדיוק את ההבדל בין "שחור" (Clear נמוך) ל"בלונד" (Clear גבוה).

### Dispenser.ino (מנוע התוספות/קרוסלה — ULN2003, יוניפולרי)
| פונקציה | אחראית על |
|---|---|
| `dispenserSetup()` | מגדירה את 4 פיני הסלילים כ-OUTPUT ומכבה אותם |
| `applyPhase(phase)` | מדליקה את הסלילים לפי טבלת הפאזות (`PHASE_SEQ`) התואמת לפאזה הנוכחית |
| `releaseCoils()` | מכבה את כל 4 הסלילים (חוסך חום כשהמנוע לא בתנועה) |
| `stepOnce(dir)` | פסיעה בודדת — מקדמת/מורידה פאזה במעגליות ומפעילה את `applyPhase()` |
| `dispenserGoTo(targetIndex)` | מחשבת כמה צעדים/לאיזה כיוון לזוז כדי להגיע למיקום התוסף המבוקש (או "none"), ומבצעת את התנועה צעד-צעד |

**זרימת לוגיקה (`PHASE_SEQ`):** כל שורה בטבלה = פאזה שבה 2 מתוך 4 סלילים דלוקים (1) והשאר כבויים (0). מעבר רציף בין השורות (0→1→2→3→0...) יוצר שדה מגנטי מסתובב שמניע את הרוטור צעד-צעד; כיוון (עולה/יורד בטבלה) קובע קדימה/אחורה.

---

## 🟢 second_esp32 — לוח המאסטר (מסך, מנועים, Firebase, שרת HTTP לאפליקציה)

### second_esp32.ino (הקובץ הראשי)
| פונקציה | אחראית על |
|---|---|
| `setup()` | מאתחלת Serial, מסך (`displaySetup`), מנועים (`motorsSetup`), UART (`uartSetup`), Firebase (`firebaseSetup`), שרת HTTP (`webServerSetup`), ומאפסת את הקרוסלה למיקום התחלתי |
| `loop()` | קוראת שוב ושוב ל-`runSession()` בלבד — שרת ה-HTTP רץ במקביל ב-task נפרד (Core 0), לא כאן |
| `runSession()` | **מכונת המצבים המרכזית של הפרויקט** — ר' זרימה מפורטת למטה |
| `colorNameToIndex(name)` | ממירה שם צבע (מזוהה ע"י חיישן הצבע) לאינדקס קרוסלת התוספות המתאים (לשימוש עם `requestDispenserGoTo()`) |

**זרימת `runSession()` — שלב אחר שלב:**
1. **קבלת קוד** — לולאה: `uiGetCode()` (מסך מקלדת) → `fbValidateCode()` מאמתת מול Firestore. אם שגוי — הודעה וחוזר לשלב 1.
2. **הצלחה** — מציגה "Connected!" עם השם.
3. **בחירת תוספות** — `uiSelectExtensions()` מחזירה אילו תוספות (עד `MAX_EXTENSIONS`) נבחרו.
4. **הכנסת שיער** — `uiInsertHair()` ממתינה ללחיצת START.
5. **בדיקת מרחק** — לולאה: `requestDistanceOk()` עד שהמרחק בטווח התקין.
6. **זיהוי צבע (אם נבחר "כצבע שערי")** — `requestHairColor()` שולפת צבע מהלוח הראשון, `colorNameToIndex()` הופכת לאינדקס, מוצג למשתמש.
7. **הגשת תוספות** — לכל תוסף שנבחר: `requestDispenserGoTo()` (מסובבת קרוסלה בלוח הראשון) + `uiTakeExtension()` (מבקשת מהמשתמש לקחת). מחזירה קרוסלה למקום 0 בסוף.
8. **אישור לקליעה** — `uiReadyToBraid()` ממתינה ללחיצת START מפורשת.
9. **קליעה** — `uartArm()` (מפעילה ניטור חירום) → `braidWhileLowering()` (מוריד מסילה + מסובב מנוע קליעה, בו-זמנית) → `uartDisarm()`.
10. **אם חירום** — `motorsStopAll()`, `uiEmergency()` (ממתינה לאישור), `railUpHome()`, שומרת הזמנה עם status="emergency" (הקוד לא משתחרר, כדי שהלקוח ינסה שוב), חוזרת לתחילת הלולאה.
11. **אם הצליח** — `uiDone()`, `railUpHome()`, `fbSaveOrder()` (status="completed"), `fbReleaseCode()` (משחררת את הקוד לשימוש חוזר), הודעת "Saved!".

### Motors.ino (מנועי המסילה והקליעה)
| פונקציה | אחראית על |
|---|---|
| `motorsSetup()` | מגדירה את 8 פיני שני המנועים (מסילה + קליעה) כ-OUTPUT ומכבה אותם |
| `applyPhase(p,phase)` | מדליקה סלילים לפי `PHASE_SEQ` (יוניפולרי, ULN2003) — למנוע הקליעה |
| `railApplyPhase(phase)` | מדליקה סלילים לפי `RAIL_PHASE_SEQ` (ביפולרי, L298N) — למנוע המסילה |
| `releaseCoils(p)` | מכבה את כל סלילי מנוע נתון |
| `stepOnce(p,phase,dir)` | פסיעה בודדת של מנוע יוניפולרי (הקליעה) בכיוון נתון |
| `railStepOnce(dir)` | פסיעה בודדת של מנוע המסילה (ביפולרי) בכיוון נתון |
| `railMove(steps,dir)` | מזיזה את המסילה מספר צעדים נתון בכיוון נתון (למעלה/למטה), ומכבה סלילים בסיום |
| `railUpHome()` | מחזירה את המסילה למעלה **בדיוק** לפי מספר המקטעים שבאמת בוצעו (`railSegmentsDone`) — כך שגם אחרי עצירת חירום באמצע, החזרה תהיה מדויקת ולא תניח תמיד מרחק מקסימלי |
| `braidWhileLowering()` | הפונקציה המרכזית של תהליך הקליעה: מסובבת את מנוע הקליעה ומורידה את המסילה יחד, במקטעים (chunks), תוך כדי בדיקת חירום (`uartReceiveLines()`) בכל מקטע; מחזירה `true` אם הסתיים בהצלחה, `false` אם נעצר בחירום |
| `motorsStopAll()` | עצירת חירום מיידית — מכבה את כל הסלילים של שני המנועים |

### UartLink.ino (תקשורת עם הלוח הראשון)
| פונקציה | אחראית על |
|---|---|
| `uartSetup()` | מאתחלת את Serial2 (UART2, פינים 16/17, 115200 baud) |
| `uartSend(s)` | שולחת מחרוזת + `\n` ללוח הראשון |
| `parseLine(line)` | מפענחת שורה שהתקבלה מהלוח הראשון ומעדכנת את המשתנים הגלובליים המתאימים (`EMG`, `OK`, `DIST:`, `DISTOK:`, `COLOR:`, `DISPDONE`) |
| `uartReceiveLines()` | קוראת כל מה שהצטבר ב-buffer של Serial2, בונה שורות שלמות ומעבירה אותן ל-`parseLine()` — נקראת לעיתים קרובות מכל מקום בקוד כדי לא לפספס עדכונים (שונה בעבר `uartPoll`) |
| `requestDistanceOk(timeoutMs)` | שולחת `DIST?`, ממתינה לתשובה (עם timeout), ומחזירה אם המרחק שהתקבל תקין |
| `requestHairColor(timeoutMs)` | שולחת `COLOR?`, ממתינה לתשובה, ומחזירה את שם הצבע שזוהה (או "Unknown" בפסק זמן) |
| `requestDispenserGoTo(targetIndex,timeoutMs)` | שולחת `DISP:<n>` ומחכה לאישור `DISPDONE` מהלוח הראשון (או ממשיכה בכל מקרה אם עבר timeout) |
| `uartArm()` | שולחת `ARMED` — מפעילה ניטור חירום בלוח הראשון, ומאפסת את הדגל המקומי |
| `uartDisarm()` | שולחת `DISARM` — מכבה ניטור חירום בלוח הראשון |

### DisplayManager.ino (מסך המגע TFT9341)
| פונקציה | אחראית על |
|---|---|
| `displaySetup()` | מאתחלת את המסך והמגע |
| `clearScreen()` | מנקה את המסך (רקע אחיד) |
| `waitRelease()` | ממתינה עד שהמשתמש מרים את האצבע מהמסך (מונעת קליטת אותה נגיעה פעמיים) |
| `uiGetCode()` | מציגה מקלדת מספרית על המסך וקוראת קוד באורך `CODE_LENGTH` שהמשתמש מקליד |
| `drawExtButton(i,selected)` | מציירת כפתור בודד למסך בחירת התוספות (Blonde/Green/Red/Black), במצב נבחר/לא-נבחר |
| `uiSelectExtensions(name,tokensOut)` | מסך בחירת תוספות — מציגה אפשרויות, קולטת נגיעות, ומחזירה אילו תוספות נבחרו |
| `extName(token)` | ממירה אינדקס תוסף (`EXT_BLONDE`/`EXT_GREEN`/`EXT_RED`/`EXT_BLACK`) לשם טקסטואלי להצגה |
| `oneButtonScreen(...)` | מסך גנרי עם כפתור אחד (טקסט + צבע מותאמים) וממתינה ללחיצה |
| `fullColorScreen(...)` | מסך גנרי במסך-מלא בצבע רקע נתון עם טקסט (למסכי סטטוס/הודעה) |
| `waitOneButton()` | ממתינה ללחיצה על הכפתור היחיד שעל המסך, תוך כדי `uartReceiveLines()` (לא לפספס חירום/אישור) |
| `uiInsertHair()` | מציגה הנחיה למשתמש להכניס שיער למכשיר |
| `uiMessage(l1,l2)` | מציגה הודעת טקסט כללית בת שתי שורות |
| `uiTakeExtension(colorName)` | מציגה הנחיה לקחת את התוסף בצבע שזוהה |
| `uiReadyToBraid()` | מסך "מוכן לקליעה" לפני תחילת התהליך |
| `uiEmergency()` | מסך התראת חירום — ממתינה ל"OK" מהכפתור הפיזי (`uartReceiveLines()`) **או** ממגע במסך |
| `uiDone()` | מסך סיום מוצלח של התהליך |

### FirebaseManager.ino (WiFi + Cloud Firestore — קוד/הזמנה, לא הרשמה/כניסה)
| פונקציה | אחראית על |
|---|---|
| `fsStr(doc,field)` | מחלצת שדה טקסט (`stringValue`) ממסמך Firestore |
| `fsBool(doc,field)` | מחלצת שדה בוליאני (`booleanValue`) ממסמך Firestore |
| `fsInt(doc,field)` | מחלצת שדה מספרי (`integerValue`) ממסמך Firestore |
| `firebaseSetup()` | מתחברת ל-WiFi, נכנסת לחשבון המכשיר ב-Firebase (או יוצרת אותו אם לא קיים), וקובעת האם המערכת במצב "אמיתי" או "דמו" (`fbReady`) |
| `fbValidateCode(code,name,uid)` | בודקת מול Firestore (או במצב דמו: בודקת רק אורך) אם הקוד קיים ולא נוצל, וממלאת שם+uid |
| `fbReleaseCode(code)` | **מוחקת** לגמרי את מסמך `codes/{code}` (לא רק מסמנת `used=true`) — כך שהמספר בן 4 הספרות חוזר להיות פנוי לשימוש חוזר (יש רק 9000 קומבינציות אפשריות) |
| `appendOrderJson(doc,docName,out)` | הופכת מסמך הזמנה בודד מ-Firestore ל-JSON פשוט (`{id,uid,name,extensions,hairColor,status,createdAt}`) ומצרפת אותו למחרוזת פלט |
| `fbListOrders(uidFilter)` | שולפת את כל מסמכי `orders` מ-Firestore (`listDocuments`); אם `uidFilter` לא ריק — מסננת רק את ההזמנות של אותו משתמש (ללקוח), אחרת מחזירה הכל (למנהל). מחזירה מחרוזת JSON `"[{...},{...}]"` מוכנה לשליחה לאפליקציה |
| `fbDeleteOrder(orderId)` | מוחקת מסמך הזמנה בודד (`orders/{orderId}`) — לשימוש ע"י לקוח/מנהל שמוחקים פריט מההיסטוריה |
| `fbSaveOrder(uid,name,extensions,hairColor,status)` | שומרת מסמך הזמנה חדש תחת `orders/{id}` עם סטטוס "completed" או "emergency" |

### AuthManager.ino (הרשמה/כניסה/קוד זמני — כל התקשורת מול Firebase Auth+Firestore בשביל האפליקציה)
| פונקציה | אחראית על |
|---|---|
| `fsStrGet(doc,field)` | מחלצת שדה טקסט ממסמך Firestore (זהה ל-`fsStr` אך עם `FirebaseData` נפרד — `authFbdo` — כדי לא להתנגש עם `fbdo` הראשי שמשמש את זרימת הקליעה) |
| `authRest(endpoint,email,pass,uidOut,errOut)` | קוראת ל-Firebase Auth REST API (`identitytoolkit.googleapis.com/v1/accounts:signUp` או `:signInWithPassword`) דרך `HTTPClient`; מחלצת `localId` (uid) מהתשובה או הודעת שגיאה |
| `authRegister(email,pass,name,uidOut,errOut)` | הרשמה: יוצרת חשבון Auth דרך `authRest("signUp",...)`, ואז יוצרת מסמך `users/{uid}` ב-Firestore עם name/email/role="user" |
| `authLogin(email,pass,uidOut,nameOut,roleOut,errOut)` | כניסה: מאמתת מול Auth דרך `authRest("signInWithPassword",...)`, ואז שולפת name+role ממסמך `users/{uid}` (או ברירת מחדל אם המסמך לא קיים) |
| `authGenerateCode(uid,name)` | הרשמת קוד זמני: מגרילה מספר 4 ספרות, בודקת מול Firestore שהוא לא תפוס (עד 10 ניסיונות), ושומרת מסמך `codes/{code}` עם uid/name/used=false |

### WebServer.ino (שרת HTTP — כל תקשורת האפליקציה עוברת דרך ה-ESP32, לא ישירות ל-Firestore)
| פונקציה | אחראית על |
|---|---|
| `sendJson(code,body)` | שולחת תשובת HTTP עם קוד סטטוס + JSON, כולל כותרות CORS (`Access-Control-Allow-*`) |
| `handleCors()` | מטפלת בבקשות `OPTIONS` (preflight CORS) — מחזירה `{}` ריק |
| `bodyField(d,field)` | מחלצת שדה בודד מגוף הבקשה (JSON) שהאפליקציה שלחה |
| `handleRegister()` | endpoint `POST /register` — קוראת שדות email/password/name, קוראת ל-`authRegister()`, מחזירה `{uid,name,role}` |
| `handleLogin()` | endpoint `POST /login` — קוראת שדות email/password, קוראת ל-`authLogin()`, מחזירה `{uid,name,role}` |
| `handleGenerateCode()` | endpoint `POST /generate-code` — קוראת שדות uid/name, קוראת ל-`authGenerateCode()`, מחזירה `{code}` |
| `handleMyOrders()` | endpoint `GET /my-orders?uid=` — קוראת ל-`fbListOrders(uid)`, מחזירה מערך ההזמנות של אותו משתמש |
| `handleAllOrders()` | endpoint `GET /all-orders` — קוראת ל-`fbListOrders("")` (בלי סינון), מחזירה את כל ההזמנות (למנהל) |
| `handleDeleteOrder()` | endpoint `POST /delete-order` — קוראת שדה orderId, קוראת ל-`fbDeleteOrder()`, מחזירה הצלחה/שגיאה |
| `webServerTask(param)` | לולאת ה-task שרצה על **Core 0** בנפרד מהלולאה הראשית (`runSession` על Core 1) — קוראת שוב ושוב ל-`authServer.handleClient()` כדי שהשרת יגיב תמיד, גם באמצע קליעה |
| `webServerSetup()` | רושמת את כל ה-routes (POST/GET/OPTIONS לעיל), מפעילה את השרת, ומריצה את `webServerTask` ב-task נפרד עם `xTaskCreatePinnedToCore` |

**זרימת לוגיקה (בקשה מהאפליקציה):** React App → `fetch()` ל-IP של ה-ESP32 → מגיע ל-`webServerTask` (Core 0, רץ תמיד ברקע) → מפעיל handler מתאים → קורא לפונקציית `auth*`/`fb*` → מחזיר JSON → App מקבלת תשובה. זה קורה **תמיד**, גם אם הלוח באמצע `runSession()` חוסם (קליעה וכו'), כי שני החלקים רצים על ליבות נפרדות.

---

## 🟣 react-app/src/api.js (שכבת הנתונים באפליקציה)
| פונקציה | אחראית על |
|---|---|
| `toSortMs(createdAt)` | ממירה `createdAt` (מספר millis() מה-ESP32) למספר אחיד למיון הזמנות |
| `esp32Post(path,payload)` | שולחת בקשת `POST` ל-ESP32 (עם JSON body), זורקת שגיאה אם `res.ok` false |
| `esp32Get(path)` | שולחת בקשת `GET` ל-ESP32 (עם query params ב-path), זורקת שגיאה אם `res.ok` false |
| `api.register(email,pass,name)` | קוראת ל-`esp32Post("/register",...)` |
| `api.login(email,pass)` | קוראת ל-`esp32Post("/login",...)` |
| `api.logout()` | ניקוי מקומי בלבד (אין session בצד השרת) |
| `api.generateCode(uid,name)` | קוראת ל-`esp32Post("/generate-code",...)`, מחזירה את הקוד |
| `api.getMyOrders(uid)` | קוראת ל-`esp32Get("/my-orders?uid=...")`, ממיינת לפי `createdAt` |
| `api.getAllOrders()` | קוראת ל-`esp32Get("/all-orders")`, ממיינת לפי `createdAt` |
| `api.deleteOrder(orderId)` | קוראת ל-`esp32Post("/delete-order",{orderId})` |

> **חשוב:** כל 7 הפונקציות ב-`api` עוברות עכשיו דרך ה-ESP32 (endpoints ב-WebServer.ino) — **אין** יותר גישה ישירה ל-Firestore מהאפליקציה. ה-imports של Firebase SDK (auth/firestore) הוסרו מהקובץ כי אינם בשימוש.

---

## 🟠 esp32_firebase — ארכיטקטורה ישנה (לוח יחיד + React, לא בשימוש פעיל)

### esp32_firebase.ino
`setup()`, `loop()` — מריצות WiFi, Firebase, שרת HTTP, מסך.

### WiFiManager.ino
| פונקציה | אחראית על |
|---|---|
| `wifiConnect()` | מתחברת לרשת WiFi |
| `wifiIsConnected()` | בודקת סטטוס חיבור |

### FirebaseManager.ino
פונקציות CRUD גנריות מול Firestore: `firebaseInit`, `firebaseIsReady`, `firebaseGetUID`, `firebaseCreateUser`, `firebaseGetDocument`, `firebasePatchDocument`, `fsStr`, `firebaseCreateAppointment`, `firebaseGetAppointments`, `firebaseCancelAppointment`, `firebaseUpdateAppointmentStatus`, `firebaseLogEvent`, `firebaseGetLogs`, `firebaseGetBusySlots`, `firebaseSignUp`, `firebaseSignIn` — ניהול משתמשים, תורים ולוגים במסד נתונים אחד עבור אפליקציית React.

### WebServer.ino
שרת HTTP מקומי על הלוח שחושף REST endpoints לאפליקציית React: `serverSendJson`, `handleOptions` (CORS), `handleLogin`, `handleRegister`, `handleLogout`, `webServerBegin`, `handleAppointment`, `handleGetAppointments`, `handleStart`, `handleStop`, `handleGetSlots`, `handleGetLogs`, `handleCancelAppointment`, `webServerHandle`.

### AppointmentManager.ino
ניהול תור פעיל בעזרת **דגל מצב מפורש** (`gActiveAppt.active`) — בניגוד ל-`second_esp32` החדש: `appointmentStart`, `appointmentStop`, `appointmentCheckTouch`, `appointmentIsActive`, `appointmentGetActiveId`.

### DisplayManager.ino
מסכי סטטוס לגרסה הישנה: `displayInit`, `displayShowStatus`, `displayShowUID`, `displayShowUser`, `displayClearUser`, `displayShowError`, `displayShowActiveAppointment`, `displayShowEmergency`, `displayShowIdle`, `displayGetTouch`.

---

## ⚪ סקיצות בדיקה (לפיתוח בלבד, לא חלק מהמערכת הסופית)

- **pin_test.ino** — `setup()`, `loop()`: בדיקת פין בודד (חומרה) בבידוד.
- **uart_hello_test.ino** — `setup()`, `loop()`: בדיקת תקשורת UART בסיסית בין שני לוחות (שליחת "Hello" מחזורית).
- **esp32_board_test.ino** — `setup()`, `loop()`, `shuttDownPins()`: בדיקת כל פיני הלוח וכיבוי מסודר שלהם.
- **color_sensor_test/color_sensor_test.ino** — `setup()`, `loop()`: בדיקה מבודדת לחיישן TCS34725 — מדפיסה RGB מנורמל (`getRGB`) וגם RAW (`getRawData`, כולל Clear) כל 500ms, לכיול והשוואת ערכים.
